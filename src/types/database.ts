// Types générés/alignés manuellement sur le schéma Supabase (voir étape 2 pour les tables réelles).
// Placeholder minimal pour que le client typé compile dès l'étape 1 ; sera remplacé par
// `supabase gen types typescript` une fois les tables créées.
export type Database = {
  public: {
    Tables: Record<string, never>
    Views: Record<string, never>
    Functions: Record<string, never>
    Enums: Record<string, never>
  }
}
